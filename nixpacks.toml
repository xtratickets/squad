providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = [
  "npm install --no-optional --ignore-scripts",
  "cd frontend && npm install --ignore-scripts"
]

[phases.build]
cmds = [
  "npx prisma generate",
  "rm -rf dist && rm -rf frontend/dist && npm run build"
]

[start]
cmd = "npx prisma migrate deploy && npm run start:prod"